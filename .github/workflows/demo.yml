name: demo

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Hello world!!!"
    - uses: actions/checkout@main
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}